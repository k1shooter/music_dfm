name: progress_like
full_graph_mode: null
baseline_graph_mode: progress_like

hidden_dim: 192
num_layers: 4
num_heads: 4
dropout: 0.1
parameterization: posterior
use_long_context_block: false

loss:
  beta_aux: 0.25
  gamma_music: 0.05
  compat_loss: false
